<div class="col-md-4">
	<h2>JSON Output</h2>
	<p>You can also make changes to the JSON here and set the value in the editor by clicking</p>
	<button class="btn btn-default btn-block" id="setvalue">Update Form</button>
	<br>
	<textarea id="output" style="width: 100%; height: 300px; font-family: monospace" class="form-control" ></textarea>
	<button class="btn btn-default btn-block"  onclick="output.select(),copyToClp()" type="submit">select</button>
	<hr>
	<h2>load from file</h2>
	<input type="file" name="file" id="myFile" class="inputfile" data-multiple-caption="{count} files selected" multiple />
	<label class="btn btn-default btn-block" for="myFile"><span>Choose a file</span></label>
	<button class="btn btn-default btn-block" onclick="loadFile()" type="submit">Load</button>
	<hr>
	<h2>load from URL</h2>
	<div class="form-group">
		<label for="inputurl">File URL</label>
		<input type="text" class="form-control" id="inputurl" value="https://" placeholder="Enter file name">
	</div>
	<button class="btn btn-default btn-block" onclick="loadURL()" type="submit">Download</button>
	<hr>
	<h2>Save to file</h2>
	<div class="form-group">
		<label for="input-fileName">File name</label>
		<input type="text" class="form-control" id="input-fileName" value="template" placeholder="Enter file name">
	</div>
	<button class="btn btn-default btn-block" onclick="saveJson()" type="submit">Download</button>
	<hr>
	<h2>Options</h2>
	<div id="options_holder">
		<div>
			<label>ACE Theme</label>
			<select id="aceTheme_switcher" class="selectpicker">
			{% for item in gdata.aceData %}
				{{ macro.options(item) }}
			{% endfor %}
			</select>
		</div>
		<hr>
		<div>
			<label>Object Layout</label>
			<select id="object_layout" class="selectpicker">
			{% for item in gdata.layoutData %}
				{{ macro.options(item) }}
			{% endfor %}
			</select>
		</div>
		<hr>
		<div>
			<label>Show Errors</label>
			<select id="show_errors" class="selectpicker">
			{% for item in gdata.errorsData %}
				{{ macro.options(item) }}
			{% endfor %}
			</select>
		</div>
		<hr>
	</div>
	<h2>Validation</h2>
	<p>This will update whenever the form changes to show validation errors if there are any.</p>
	<textarea id="validate" readonly="readonly" disabled="disabled" class="form-control"></textarea>
</div>